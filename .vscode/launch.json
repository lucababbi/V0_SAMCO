{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "python",         // Make sure this is "python"
            "request": "launch",
            "justMyCode": false,       // Set to false to include all code (not just yours)
            "program": "${file}",      // Program to debug
            "console": "integratedTerminal"
        }
    ]
}
